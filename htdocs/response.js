class responses{
  constructor(){

  }

  /* We need a function we can call to give back the correct response.
  * We need, as parameters, the objects corresponding to the actions.
  * This so we can call from here the actions, like a wrapper.
  */

  getResponse(weather){
    
  }
}
